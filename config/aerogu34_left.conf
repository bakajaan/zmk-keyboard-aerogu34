# Battery
CONFIG_ZMK_BATTERY_REPORTING=y
# 【2026-07-26 復活】標準バッテリーサービス(BAS)を有効化 (右側と統一)。
# セントラル(右)が左の残量を取得する仕組み(BATTERY_LEVEL_FETCHING)は
# 左側BASキャラクタリスティックの購読のため、無効化すると左の残量が取れず
# 0%表示の原因になる。=n には戻さないこと。
CONFIG_BT_BAS=y

# RGB LED widget
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_HIGH=40
CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_LOW=20

# ========================================
# DYA Studio モジュール (Peripheral側)
# ========================================
# バッテリー履歴 (ペリフェラル側でデータ収集)
CONFIG_ZMK_BATTERY_HISTORY=y

# 設定RPC (セントラルからの設定変更を受信)
CONFIG_ZMK_SETTINGS_RPC=y

# Split event relay (peripheral -> central)
CONFIG_ZMK_SPLIT_RELAY_EVENT=y

# ウォッチドッグ (フリーズ検知・ログ記録 / 左側MCU専用。
# デバッグ中の一時無効化は2026-07-26に解除済み)
CONFIG_ZMK_WATCHDOG=y

# キースイッチ診断 (左側キーのチャタリング検出・押下統計)
CONFIG_ZMK_KSCAN_DIAGNOSTICS=y
# Split: 左側の診断データをセントラル経由でDYA Studioへ中継
CONFIG_ZMK_KSCAN_DIAGNOSTICS_SPLIT=y
# 中継ペイロードは1トポロジーページ分必要 (両halves共通)
CONFIG_ZMK_SPLIT_RELAY_EVENT_DATA_LEN=256

# 設定永続化
CONFIG_ZMK_SETTINGS_SAVE_DEBOUNCE=60000

# ========================================
# BLE
# ========================================
# TX出力+8dBm (jisaku_1と同設定: 左右間リンク安定化)
CONFIG_BT_CTLR_TX_PWR_PLUS_8=y

# スリープ復帰後などにホスト側の購読状態が食い違って入力が届かなくなる問題のワークアラウンド
CONFIG_BT_GATT_ENFORCE_SUBSCRIPTION=n

# ========================================
# 省電力
# ========================================
# バッテリー報告間隔を 10分に (jisaku_1と同設定)
CONFIG_ZMK_BATTERY_REPORT_INTERVAL=600

# ソフトオフ(ScrollLayerのU+I+Oコンボで電源断、jisaku_1と同設定)
CONFIG_ZMK_PM_SOFT_OFF=y

# スリープ有効化（セントラルのディープスリープ移行に連動して確実にスリープするために明示）
# セントラル(右)の IDLE_SLEEP_TIMEOUT=3600000ms(60分)と統一
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=3600000

# ========================================
# キースキャン (jisaku_1と同設定)
# ========================================
# 低遅延デバウンス(eager debounce: 押下1ms＋解放5ms、検出遅延を約4ms短縮)
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=1
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5
